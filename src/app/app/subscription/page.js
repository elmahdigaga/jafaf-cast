import Wrapper from '@/components/app/wrapper'

export default async function AccountPage() {
  return (
    <Wrapper>
      <div className="space-y-12">User Subscription</div>
    </Wrapper>
  )
}

export const metadata = {
  title: 'Subscription',
}
